# SimulationControlProjects
present : PID-controlled inverted pendulum and basic pendulum simulations ; Upcoming : LQR and MPC for InvertedPendulum , missile tracking etc.

inorder to run install the requirements.txt by pip install -r requirements.txt 
thne use the command python -m uvicorn main:app in order to run uvicorn on your local machine.
